Architecture
============

This project is a React-based web application with a Supabase backend.

Frontend
--------

The frontend is built with:

- React
- Vite
- React Router
- CSS for styling

The main user interface is organized into components such as:

- Sign in and sign up pages
- Supervisor listing views
- Project idea management pages
- Profile pages

Backend and Data
----------------

The application uses Supabase for:

- Authentication
- Database storage
- User and project-related data management

This allows the app to manage supervisors, and project ideas without building a custom server.

Project Structure
-----------------

Key folders include:

- src/components: reusable UI components
- src/context: authentication context
- docs: Sphinx documentation files
